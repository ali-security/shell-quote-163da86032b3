name: 'Tests: pretest/posttest'

on: [pull_request, push]

permissions:
  contents: read

jobs:
  tests:
    if: false  # disabled on the seal fork: lint-only (pretest) + `npm audit` (posttest), runs no tests
    uses: ljharb/actions/.github/workflows/pretest.yml@main
